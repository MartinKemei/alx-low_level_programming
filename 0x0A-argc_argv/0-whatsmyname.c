#include <stdio.h>

/**
 * main - prints its name in a new line
 * @argc: argument count
 * @argv: pointers to arguments
 *
 * Return: 0
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
