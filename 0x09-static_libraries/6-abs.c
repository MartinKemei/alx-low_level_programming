#include "main.h"

/**
 * _abs - absolute value of an integer
 * @n: int to be computed
 *
 * Return: absolute value of an integer
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
