#include "main.h"

/**
 *print_number - a function that prints an integer
 *
 *@n: argument parameter
 *Return: void
 */

void print_number(int n)
{
	if (n < 0)
	{
		_putchar('-');
		return;
	}
	else if (n == 0)
	{
		_putchar('0');
	}
	else
		_putchar(n + '0');
}
