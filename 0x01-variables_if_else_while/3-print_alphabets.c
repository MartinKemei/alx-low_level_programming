#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char b;
	char c;

	while
		(b <= 'a') {
			putchar(b);
			b++;
		}
	while
		(c <= 'A') {
			putchar(c);
			c++;
		}
	return (0);
}

